using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace testLINQ
{
    public class LetterLINQprobem
    {
       

        public static void Lettercount()
        {
           // string testString = "hello";
           //var  testlist= testString.Split();
           // testlist.Where(i => i.Count()==testlist.Count());
           // var newTest = testString.Where(i => i.Count()) && testString.Select(i);
           // newTest.OrderbyDescending();

           // var letters = testString.ToUpper().OrderBy(t => t).ToList();
           // var distinctletters = letters.Distinct().ToList();


        }

    }
}
